<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.algorizo.erp.chatMapper">
    <select id="getMessagesByRoomId" resultType="co.algorizo.erp.chat.ChatMessageDTO">
        SELECT * FROM chat_message
        WHERE room_id = #{room_id}
        ORDER BY sent_at ASC
    </select>

    <insert id="insertMessage" parameterType="co.algorizo.erp.chat.ChatMessageDTO">
        INSERT INTO chat_message(room_id, sender_id, message_content, sender_name, room_password)
        VALUES(#{room_id}, #{sender_id}, #{message_content}, #{sender_name}, #{room_password})
    </insert>
    <select id="getChatRoomList" resultType="co.algorizo.erp.chat.ChatMessageDTO">
        SELECT room_id
        FROM chat_message
        GROUP BY room_id
    </select>
    <select id="getChatRoomPassword" resultType="String">
        SELECT room_password FROM chat_message WHERE room_id = #{room_id} LIMIT 1
    </select>

	
	
</mapper>